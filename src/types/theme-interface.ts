export interface Theme {
  name: string;
  mainColor: string;
  backgroundColor: string;
  navigatorColor: string;
  navigatorHoverColor: string;
  navigatorActiveColor: string;
  borderColor: string;
}
